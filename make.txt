# Makefile for UNIX        

# the compiler: gcc for C program, define as g++ for C++ 
CC = gcc  

# compiler flags: 
# -g enable debugging 
# -Wall turn on all warnings 
# -D__USE_FIXED_PROTOTYPES__  force the compiler to use the correct headers 
# -ansi don’t use GNU extensions, stick to ANSI
# -std=c99 use newer version
CFLAGS=-g -Wall -D__USE_FIXED_PROTOTYPES__ -std=c99 

# the build target executable: 
TARGET = client

all: $(TARGET)

$(TARGET): client.o helper.o conn_socket.o
	$(CC) $(CFLAGS) -o client main.c client.o conn_socket.o helper.o

client.o: client.c client.h
	$(CC) $(CFLAGS) -c client.c

conn_socket.o: networking/conn_socket.c networking/conn_socket.h
	$(CC) $(CFLAGS) -c networking/conn_socket.c

helper.o: common/helper.c common/helper.h
	$(CC) $(CFLAGS) -c common/helper.c

clean:
	$(RM) $(TARGET) $(TARGET).o
